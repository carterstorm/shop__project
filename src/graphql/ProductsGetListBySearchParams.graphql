query ProductsGetListBySearch($search: String) {
	products(search: $search) {
		data {
			id
			name
			price
			description
			rating
			images {
				url
			}
			categories {
				name
				description
			}
		}
	}
}
