query CategoryGetItemById($slug: String) {
	category(slug: $slug) {
		slug
		products {
			id
			name
			price
			description
			images {
				url
			}
			categories {
				name
				description
			}
		}
	}
}
